import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Timeout;

class TaskTest {
    Task t = new Task();

    @Test
    @Timeout(2) // fails if takes more than 2 sec
    void testTimeout() throws Exception {
        t.longRunningTask();
    }
}
